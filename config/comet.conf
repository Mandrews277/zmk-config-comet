# Disable Bluetooth - USB only mode
CONFIG_BT=n
CONFIG_ZMK_BLE=n

# Disable deep sleep to prevent disconnection issues
CONFIG_ZMK_SLEEP=n

# Enable USB logging for debugging
CONFIG_ZMK_USB_LOGGING=y

# Force USB mode
CONFIG_ZMK_USB=y
